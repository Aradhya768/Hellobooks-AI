# Hellobooks AI — Python dependencies
# Install with: pip install -r requirements.txt

# Core RAG stack (all free, runs locally — no API key needed)
sentence-transformers>=2.7.0
faiss-cpu>=1.8.0
numpy>=1.26.0
requests>=2.31.0         # Talks to Ollama local LLM server

# Optional: only needed if you switch to paid providers
# openai>=1.30.0         # Only for OpenAI API (paid)
# python-dotenv>=1.0.0   # Load .env files
